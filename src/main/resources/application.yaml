spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/shopApp
    username: root
    password: Ss55539927
  flyway:
    baseline-on-migrate: true
